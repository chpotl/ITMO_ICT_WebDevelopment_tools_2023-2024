1) Insert your link into the .env file without quotes
2) Run alembic.ini from the console
3) Replace the env.py file in the migrations folder with ENV_TO_REPLACE (just rename it)
4) In script.py.mako, import sqlalchemy and sqlmodel as the first lines
5) Launch using uvicorn
